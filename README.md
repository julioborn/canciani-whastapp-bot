# Canciani WhatsApp Bot Starter

## Setup
1. Install dependencies:
   npm install

2. Create .env file:
   Copy .env.example to .env and add your API key.

3. Start server:
   npm start

## Webhook
POST /webhook

Expose locally with ngrok:
ngrok http 3000